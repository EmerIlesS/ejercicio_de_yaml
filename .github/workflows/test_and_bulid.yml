name: Test and Build  
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
  Test:
    runs-on: ubuntu-latest
    steps:
      - name: 'hello world'
        run: echo "Hello, world!"
      - name: fecha
        run: date
      - name: muestreme el contenido del directorio
        run: |
          ls -la